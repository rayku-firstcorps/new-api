# Stripe 退款后按订单扣减用户余额方案

## 背景

当前 Stripe 普通充值订单的本地关联键是 `top_ups.trade_no`，该值会写入 Stripe Checkout Session 的 `client_reference_id`。支付成功后，Stripe webhook 通过 `client_reference_id` 找回本地订单，并在本地事务中把 `top_ups` 标记为成功，同时增加用户 `quota`。

现有代码没有保存 Stripe 的 `payment_intent_id`、`charge_id` 或 `refund_id`，也没有现成的“按 `top_ups.id` 执行退款扣余额”接口。

## 当前可用方案：管理员接口扣余额

系统已有管理员额度调整接口：

```http
POST /api/user/manage
```

请求示例：

```json
{
  "id": 123,
  "action": "add_quota",
  "mode": "subtract",
  "value": 5000000
}
```

字段含义：

- `id`：用户 ID，不是订单 ID。
- `action`：固定传 `add_quota`。
- `mode`：扣余额传 `subtract`。
- `value`：扣减的 quota 数量。

这个接口会调用 `model.DecreaseUserQuota(user.Id, value, true)`，会更新数据库和 Redis 用户额度缓存，并写入管理员操作日志。因此，如果不改代码，推荐使用这个接口扣余额。

## 通过订单 ID 计算扣减额度

先用订单 ID 查询 `top_ups`：

```sql
SELECT id, user_id, amount, money, trade_no, payment_method, payment_provider, status
FROM top_ups
WHERE id = 订单ID;
```

普通 Stripe 充值的入账逻辑是：

```text
增加额度 = top_ups.money * common.QuotaPerUnit
```

项目中 `common.QuotaPerUnit = 500000`，所以 Stripe 全额退款时应扣：

```text
扣减额度 = top_ups.money * 500000
```

示例：

```text
top_ups.money = 10
扣减额度 = 10 * 500000 = 5000000
```

然后调用管理员接口：

```json
{
  "id": top_ups.user_id,
  "action": "add_quota",
  "mode": "subtract",
  "value": 5000000
}
```

## 不同支付类型的额度计算

不同支付渠道的 `top_ups.amount` 和 `top_ups.money` 语义不完全相同：

| 支付类型 | 扣减额度 |
| --- | --- |
| Stripe 普通充值 | `top_ups.money * 500000` |
| EPay | `top_ups.amount * 500000` |
| Waffo | `top_ups.amount * 500000` |
| Waffo Pancake | `top_ups.amount * 500000` |
| Creem | `top_ups.amount` |
| Stripe 订阅 | 不应直接扣 `users.quota`，应处理 `user_subscriptions` |

本方案主要针对 Stripe 普通充值退款。

## 数据库直接扣减方案

不推荐直接改数据库，但紧急情况下可以执行：

```sql
UPDATE users
SET quota = quota - 5000000
WHERE id = 123;
```

直接改数据库的风险：

- 如果 Redis 用户缓存开启，缓存里的 quota 可能仍是旧值。
- 不会写管理员操作日志。
- 不会记录退款和扣减之间的审计关系。

因此数据库方式只适合作为兜底。执行后应同步清理或刷新该用户缓存，并补写操作日志。

## 推荐正式实现

新增一个管理员退款扣余额接口，例如：

```http
POST /api/user/topup/refund
```

请求体：

```json
{
  "topup_id": 1001,
  "refund_amount": 10,
  "reason": "stripe refund"
}
```

后端处理流程：

1. 校验管理员权限。
2. 根据 `topup_id` 查询并锁定 `top_ups` 订单。
3. 校验订单属于可退款状态，例如 `status = success`。
4. 校验支付渠道，Stripe 普通充值按 `money * QuotaPerUnit` 计算扣减额度。
5. 支持全额退款和部分退款：

```text
全额扣减 = top_ups.money * 500000
部分扣减 = top_ups.money * 500000 * refund_amount / paid_amount
```

6. 在事务内扣减 `users.quota`。
7. 记录退款状态，避免重复扣减。
8. 写管理员操作日志。
9. 同步更新 Redis 用户额度缓存。

## 建议新增字段

如果后续要和 Stripe Refund API 自动联动，建议给支付流水增加字段，或新建 `payment_transactions` / `refund_records` 表。

至少需要记录：

```text
trade_no
provider
checkout_session_id
payment_intent_id
charge_id
refund_id
paid_amount
currency
refund_amount
refund_status
refunded_quota
operator_id
reason
created_at
updated_at
```

如果继续复用 `top_ups`，也建议新增：

```text
refund_status
refund_amount
refunded_quota
refund_time
refund_reason
```

## 余额不足策略

退款扣余额时可能出现用户已经消费完余额的情况。建议采用以下规则：

- Stripe 后台已经退款成功，系统必须扣减余额，即允许 `users.quota` 变成负数。
- 管理员主动发起退款前，可以提示当前余额不足，但不阻止扣减。
- 负数余额会自然阻止用户继续消费，直到后续充值补足。

这样可以保证 Stripe 财务状态和本地账务状态一致。

## 当前最稳操作流程

在不改代码的情况下：

1. Stripe 后台完成退款。
2. 用本地订单 ID 查询 `top_ups`。
3. 确认是普通 Stripe 充值订单。
4. 计算扣减额度：`money * 500000`。
5. 用管理员账号调用 `/api/user/manage`，`mode = subtract`。
6. 在备注或日志中记录 Stripe 退款 ID 和本地订单号。

## 外部系统接入方案

如果另一套系统需要接入本项目完成“按订单扣减用户余额”，建议优先走 API，其次才考虑直连数据库。

### 方案 A：通过现有 API 接入

外部系统可以使用管理员账号登录后，调用现有接口扣减用户余额。

接入流程：

1. 外部系统保存或获取本项目管理员登录态。
2. 外部系统根据本地订单 ID 查询 `top_ups`。
3. 外部系统按支付渠道计算应扣 quota。
4. 外部系统调用 `/api/user/manage` 扣减用户余额。
5. 外部系统保存本次退款处理记录，避免重复扣减。

扣减接口：

```http
POST /api/user/manage
Content-Type: application/json
Cookie: 管理员登录 Cookie
```

请求体：

```json
{
  "id": 123,
  "action": "add_quota",
  "mode": "subtract",
  "value": 5000000
}
```

优点：

- 会同步更新数据库和 Redis 用户额度缓存。
- 会写管理员操作日志。
- 不需要外部系统了解本项目 Redis 缓存细节。
- 不需要外部系统直接写核心业务表。

缺点：

- 现有接口只接收 `user_id` 和 `value`，不接收 `topup_id`。
- 现有接口日志里不会自动记录 Stripe `refund_id`、`topup_id`、`trade_no`。
- 外部系统需要自己保证幂等。

### 方案 B：新增专用 API 给外部系统调用

如果这是长期对接，建议新增一个内部接口，例如：

```http
POST /api/internal/topup/refund-deduct
```

请求体：

```json
{
  "topup_id": 1001,
  "external_refund_id": "re_xxx",
  "refund_amount": 10,
  "reason": "stripe refund from external system"
}
```

建议鉴权方式：

- 使用固定内部 API Key。
- 或使用 HMAC 签名。
- 或仅允许内网 IP 调用，并叠加 API Key。

建议请求头：

```http
X-Internal-Api-Key: xxx
X-Idempotency-Key: stripe-refund-re_xxx
```

后端应做的事情：

1. 校验内部接口权限。
2. 校验 `X-Idempotency-Key`，防止重复扣减。
3. 根据 `topup_id` 锁定 `top_ups` 订单。
4. 校验订单状态为 `success`。
5. 校验支付渠道为 Stripe 普通充值。
6. 按 `money * 500000` 或部分退款比例计算扣减 quota。
7. 扣减 `users.quota`。
8. 写退款记录或更新退款状态。
9. 写日志，记录 `topup_id`、`trade_no`、`external_refund_id`、扣减 quota、调用方。
10. 同步更新 Redis 用户额度缓存。

这个方案比直接调用 `/api/user/manage` 更适合自动化，因为所有订单校验、额度计算、幂等和审计都在本项目内部完成。

### 方案 C：通过 SQL 接入

外部系统也可以直连数据库操作，但这是兜底方案，不推荐作为主链路。

查询订单：

```sql
SELECT id, user_id, amount, money, trade_no, payment_provider, status
FROM top_ups
WHERE id = 1001;
```

Stripe 普通充值扣减：

```sql
UPDATE users
SET quota = quota - 5000000
WHERE id = 123;
```

建议外部系统另建一张退款处理表，记录幂等状态：

```sql
CREATE TABLE external_refund_records (
  id BIGINT PRIMARY KEY,
  topup_id BIGINT NOT NULL,
  trade_no VARCHAR(255) NOT NULL,
  user_id BIGINT NOT NULL,
  external_refund_id VARCHAR(255) NOT NULL,
  refund_amount DECIMAL(18, 6) NOT NULL,
  deducted_quota BIGINT NOT NULL,
  status VARCHAR(32) NOT NULL,
  reason TEXT,
  created_at BIGINT NOT NULL,
  updated_at BIGINT NOT NULL
);
```

幂等约束建议：

```sql
CREATE UNIQUE INDEX idx_external_refund_id
ON external_refund_records (external_refund_id);
```

SQL 方式必须注意：

- 需要自己处理事务。
- 需要自己防止重复扣减。
- 需要自己写审计日志。
- 需要处理 Redis 用户额度缓存不一致问题。
- 如果扣减失败但外部系统认为成功，会造成账务不一致。

因此 SQL 方式只建议用于一次性补账、紧急修正或没有能力改 API 的场景。

### 外部系统推荐流程

推荐最终流程：

1. 外部系统收到 Stripe 退款成功事件，拿到 `refund_id`。
2. 外部系统根据 Stripe Checkout Session 的 `client_reference_id` 找到本地 `trade_no`，或根据已保存映射找到 `topup_id`。
3. 外部系统调用本项目专用退款扣减 API。
4. 本项目内部完成订单锁定、幂等校验、扣余额、写日志。
5. 外部系统保存调用结果。

如果暂时不开发专用 API，可以临时采用：

1. 外部系统查询 `top_ups`。
2. 外部系统计算扣减 quota。
3. 外部系统调用 `/api/user/manage`。
4. 外部系统自行保存 `refund_id -> topup_id -> deducted_quota` 的处理记录。

### 接入时必须保证的规则

- 同一个 Stripe `refund_id` 只能扣减一次。
- 同一个 `topup_id` 的累计退款额度不能超过订单原支付额度。
- 只有 `status = success` 的订单才能扣减。
- Stripe 订阅订单不要直接扣 `users.quota`。
- 扣减金额和 quota 换算规则必须固定，并记录当时使用的换算结果。
- 允许扣成负数，以保证 Stripe 已退款时本地账务同步。
- 每次扣减必须有审计记录，至少包含操作者、调用方、订单号、退款号和扣减 quota。
